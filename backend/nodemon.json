{
  "watch": ["src"],
  "ext": "ts,json",
  "ignore": ["src/**/*.test.ts", "dist/**/*", "node_modules/**/*"],
  "exec": "pnpm exec ts-node --transpile-only -r tsconfig-paths/register src/main.ts",
  "env": {
    "NODE_ENV": "development"
  }
}
